/**
 * A low-discovery entry point for internal operations. This path is not an
 * authorization boundary: every request is still gated by requireSuperAdmin.
 */
export const adminConsolePath = "/admin-8c7fbacf";
